# Tenant Admin API

Owner: Aahil (Dev A)

Multi-tenant admin endpoints for uploading listings and calendar keys, built
on top of the PostgreSQL Row-Level Security isolation in `../../db/migrations/`.

- **PROP-602** — Tenant Admin API endpoints (listings + calendar key upload)
