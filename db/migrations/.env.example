# Run as the cluster superuser (default local setup) -- 000_create_app_role.sql
# needs superuser privileges to create the propview_app role. See this
# directory's README for why application code must connect as
# propview_app instead, never this superuser URL.
DATABASE_URL=postgresql://localhost/propview_dev
