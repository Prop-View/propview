# Same key as services/gemini-client/.env -- used to embed each chunk.
GEMINI_API_KEY=

# Must be the propview_app role, not a superuser -- PROP-601's RLS
# otherwise silently does nothing. See db/migrations/README.md.
DATABASE_URL=postgresql://propview_app:devpassword@localhost/propview_dev
