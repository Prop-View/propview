# PROP-108: real domain name with a DNS A record pointing at the VM's
# static IP (reserved by ../gcp/provision_vm.sh). Caddy uses this to
# request a real Let's Encrypt cert automatically -- see ./Caddyfile.
# Can't be filled in without an actual deployed VM + a domain you control.
LIVEKIT_DOMAIN=
